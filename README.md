# IOE 511 Final Project

## Setup

```bash
pip install uv        # install uv
uv venv               # create a .venv in the project root
uv sync               # install dependencies from pyproject.toml
uv run main.py        # run with the project's venv
```

To add a package:

```bash
uv add <package>
```
